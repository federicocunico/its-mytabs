services:
    app:
        # Build the image from this repository's current source.
        build:
            context: .
            dockerfile: Dockerfile
        image: its-mytabs:local
        ports:
            # Host Port:Container Port
            - "47777:47777"
        volumes:
            # Host Path:Container Path
            - ./data:/app/data
        restart: unless-stopped
