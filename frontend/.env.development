# Vite dev-mode configuration (committed defaults).
# Per-machine overrides go in .env.development.local (gitignored).

# Port the dev backend listens on; keep in sync with MYTABS_PORT when you
# run the backend on a non-default port.
VITE_BACKEND_PORT=47777
